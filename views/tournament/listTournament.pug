extends ../layout

block content
	h1 Hackathon Starter
	p.lead A boilerplate for Node.js web applications.
	hr
	.row 
		each val, index in list
			.col-sm-6
				h2= val.name
				p= val.info 
				p
					a.btn.btn-default(href='/match/list/' + val._id, role='button') View details »