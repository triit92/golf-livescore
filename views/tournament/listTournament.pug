extends ../layout

block content
	h1 Hackathon Starter
	p.lead A boilerplate for Node.js web applications.
	hr
	.row 
		each val, index in list
			.col-sm-6
				h2= val.name
				p= val.info 
				h4
					| Thể loai: 
					if val.type == 1
						|  Giải dấu
					if val.typr == 2
						|  Thi đấu
				p
					a(href='/tournament/edit/' + val._id) Edit details »
					a(href='/match/list/' + val._id) Match list »